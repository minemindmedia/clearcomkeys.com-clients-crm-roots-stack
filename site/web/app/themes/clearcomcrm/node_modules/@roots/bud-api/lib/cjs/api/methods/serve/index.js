"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.method = void 0;
const bud_support_1 = require("@roots/bud-support");
const { isFunction } = bud_support_1.lodash;
const method = function (input, options) {
    const app = this;
    if (!app.isDevelopment)
        return app;
    if (options) {
        const unwrapped = isFunction(options)
            ? options(app.hooks.filter('dev.options') ?? {})
            : options;
        app.hooks.on('dev.options', value => value ? { ...value, ...unwrapped } : unwrapped);
    }
    if (typeof input === 'string') {
        return app.hooks.on('dev.url', new URL(input));
    }
    if (input instanceof URL) {
        return app.hooks.on('dev.url', input);
    }
    return app;
};
exports.method = method;
