import { Framework } from '@roots/bud-framework';
import { Container } from '@roots/container';
import { WPThemeJson } from '..';
/**
 * Callback function used to configure wordpress `theme.json`
 *
 * @public
 */
export interface Mutator {
    (json: Container<WPThemeJson['settings']>): Container<WPThemeJson['settings']>;
}
export interface method {
    (input?: Mutator | WPThemeJson['settings'], raw?: boolean): Promise<Framework>;
}
export interface facade {
    (input?: Mutator | WPThemeJson['settings'], raw?: boolean): Framework;
}
export declare const method: method;
//# sourceMappingURL=themeJson.d.ts.map